/*
shortcut for if statements
- ternary operator
- Gaurd operator
- Defaalt operator 
*/

// ternary operator
// advantage of ternary operator can store in a variable

var result = true ? 'truth' : 'flase';
console.log(result)

// && (and operator) is also called as the guard operator
// it blocks the execution of the second variable if the first variable  written false like if(firstvarivble){second variable}


// or operator is also called as a default 