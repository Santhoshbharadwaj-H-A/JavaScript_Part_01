// imp:-  window is a built in object that represen the browser

window.console.log("window"); //it is samea as

console.log("windows") // javascript automatically adds the windows before the funcction
